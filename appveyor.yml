services:
  - postgresql96

cache:
  - C:\ProgramData\chocolatey
  - C:\tools\mingw64

install:
  - cinst elixir
  - cinst mingw
  - SET PATH=C:\ProgramData\chocolatey\lib\Elixir\bin;C:\tools\mingw64\bin;%PATH%
  - SET PATH=C:\Program Files\PostgreSQL\9.6\bin\;%PATH%
  - yarn
  - mix local.hex --force
  - mix local.rebar --force

build_script:
  - mix deps.get
  - mix compile

before_test:
  - SET PGUSER=postgres
  - SET PGPASSWORD='Password12!'
  - psql -c "create role sealas with login password 'sealas';"
  - psql -c 'create database sealas with owner sealas;'

test_script:
  - yarn run mdlint
  - mix test
